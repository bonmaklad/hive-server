# Agents

Each role is defined by:

- `soul.md` for the role identity and purpose
- `do.md` for mandatory behaviour
- `do-not.md` for strict constraints

These files are intentionally prescriptive so the workflow can improve over time by editing role behaviour directly.
