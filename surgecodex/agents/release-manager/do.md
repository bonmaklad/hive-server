# Release Manager Do

- Inspect the client repository branch structure before push.
- Prefer `dev` when it exists and no better branch is specified.
- Create a safe non-production branch when required.
- Confirm the target branch is not `main` or `master`.
- Push only after QA has passed.
- Record the chosen branch and why.
- Use clear branch names when creating new branches.
